---
title: "Real-time E-commerce Lakehouse (Showcase)"
slug: "realtime-lakehouse"
summary: "End-to-end streaming lakehouse: Kinesis → Iceberg → Snowflake → dbt → Streamlit."
role: "Solo build"
date: "2026-05-25"
stack: ["Python", "Kinesis", "Lambda", "S3", "Apache Iceberg", "AWS Glue", "Step Functions", "Snowflake", "dbt", "Streamlit", "Terraform"]
metrics:
  - { label: "Mode", value: "Streaming" }
  - { label: "Storage", value: "Open Iceberg" }
  - { label: "Stack", value: "Full lakehouse" }
featured: true
---

## Why
A reference architecture for a real-time lakehouse using managed AWS + Snowflake + dbt. Demonstrates streaming ingestion, open-table format storage, warehouse modelling, and a BI layer end-to-end — and stays cheap to run.

## Architecture
1. Mock Shopify event generator (Python Faker) streams orders to **Kinesis Data Streams**.
2. **Lambda** writes to **S3 in Iceberg format** via the Glue catalog.
3. **Step Functions** orchestrate **Glue** bronze → silver → gold jobs.
4. **Snowflake** queries Iceberg via external tables; **dbt** layers staging + marts.
5. **Streamlit** dashboard reads from Snowflake.

## Status
Architecture is finalized; build is in progress. Available for walk-through on request.
